??=include <stdio.h>
??=define f(x) fprintf(stderr, "%s\n", x)
??=define e(x) return x;
??=define v void
??=define it int
??=define m main
??=define ca char
??=define zh(x, y) printf("%zu %s\n", x, y??(1??))
??=define w while
??=define r fopen
??=define t size_t
??=define o fgetc
??=define __ FILE
??=define p	== '\n'

v _() ??<
	f("error");
??>

it m(it a3, ca* a1??(??)) ??<
	!a1[1] ? (??< _(); e(1); ??>) : 0;
	__* a = r(a1??(1??), "r");
	a == 0 ? (??< _(); e(1); ??>) : 0; it c; t d = 0;
	w ((c = o(a)) != (-1)) ??<
		(c p) ? d++ : 0;
	??>
	fclose(a);
	zh(++d, a1);
	e(0);
??>
